spring:
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: ${gi_cid}
            clientSecret: ${gi_cs}
          google:
            clientId: ${g_cid}
            clientSecret: ${g_cs}


server:
  port: 1120
